{
    "training_params": {
        "pretrained_model_ckpt": "yolo11x.pt",
        "output_dir": "./output/sample_run",
        "max_steps": 10,
        "learning_rate": 1e-4,
        "save_every": 5,
        "val_check_interval": 0.1,
        "gpu": [
            1
        ],
        "freeze_backbone": true
    },
    "data_config": {
        "batch_size": 32,
        "data_yaml_path": "sample_data/dataset.yaml"
    },
    "wandb_config": {
        "project": "torchyolo",
        "run_name": "sample_run",
        "should_log": false
    }
}